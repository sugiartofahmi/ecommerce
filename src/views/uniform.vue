<template>
    <div class="bg-white">
        <div class="max-w-2xl mx-auto py-16 px-4 sm:py-24 sm:px-6 lg:max-w-7xl lg:px-8">
            <h2 class=" flex justify-center text-2xl font-extrabold tracking-tight text-gray-900">UNIFORM</h2>

            <div class="mt-6 grid grid-cols-1 gap-y-10 gap-x-6 sm:grid-cols-2 lg:grid-cols-4 xl:gap-x-8">
                <div v-for=" item in products" :key="item.id" class="group relative">
                    <div v-if="item.category === 'uniform'">
                        <div class=" w-full min-h-80 bg-gray-200 aspect-w-1 aspect-h-1 rounded-md overflow-hidden
                        group-hover:opacity-75 lg:h-80 lg:aspect-none">
                            <img :src="item.imageSrc"
                                class="w-full h-full object-center object-cover lg:w-full lg:h-full" />
                        </div>
                        <div class="mt-4 flex justify-between">
                            <div>
                                <h3 class="text-sm text-gray-700">
                                    <a :href="item.href">
                                        <span aria-hidden="true" class="absolute inset-0" />
                                        {{ item.name }}
                                    </a>
                                </h3>
                                <p class="mt-1 text-sm text-gray-500">{{ item.color }}</p>
                            </div>
                            <p class="text-sm font-medium text-gray-900">{{ item.price }}</p>
                        </div>
                        <div class="flex justify-center">
                            <button type="button"
                                class="text-white bg-gray-800 hover:bg-gray-900 focus:outline-none focus:ring-4 focus:ring-gray-300 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 dark:bg-gray-800 dark:hover:bg-gray-700 dark:focus:ring-gray-700 dark:border-gray-700">Add
                                TO Cart</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</template>

<script setup lang="ts">
import products from "../../uniform.json";


</script>